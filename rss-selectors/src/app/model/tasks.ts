import { Task } from '../types';

const data: Task[] = [
  {
    hint: 'circle',
    structure: [
      {
        type: 'circle',
        color: 'blue',
        size: 'big',
        select: true,
      },
    ],
  },

  {
    hint: 'circle',
    structure: [
      {
        type: 'circle',
        color: 'red',
        size: 'big',
        select: true,
      },
    ],
  },
];

export default data;
